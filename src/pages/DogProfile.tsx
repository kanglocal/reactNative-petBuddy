import React, {useCallback} from 'react';
import {Text, View} from 'react-native';

function DogProfile() {
  return (
    <View>
      <Text>DogProfile</Text>
    </View>
  );
}

export default DogProfile;
